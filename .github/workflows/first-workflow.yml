name: first workflow
on:
  push:
    branches:
      - "main"
jobs:
  first-workflow-job:
    name: first job
    runs-on: ubuntu-latest
    steps:
      - name: before checkout actions
        run: ls
        shell: bash
      
      - name: checkout steps
        uses: actions/changes@6.0.2
      
      - name: after checkout actions
        run: ls
        shell: bash
